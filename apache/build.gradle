
apply plugin: 'docker'

docker {
	scalable false

	templates fileTree( '.' ) {
		include 'docker-compose.yml'
		include 'dockerfile.txt'
		include 'apache.conf'
		include 'run-httpd'
	}

	configuration {
		port = 1080
	}
}

task clean {
	doLast {
		delete buildDir
	}
}
